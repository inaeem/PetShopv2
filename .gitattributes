# Auto detect text files and perform LF normalization
* text=auto

# Curl harness shell scripts MUST stay LF, or CRLF breaks the bash shebang
# (`/usr/bin/env bash^M`) in the Git Bash / VS Code terminal on Windows.
*.sh    text eol=lf
