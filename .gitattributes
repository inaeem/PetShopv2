# Auto detect text files and perform LF normalization
* text=auto

# Curl harness is Windows-native PowerShell — keep these scripts CRLF.
*.ps1   text eol=crlf
*.cmd   text eol=crlf
*.bat   text eol=crlf
